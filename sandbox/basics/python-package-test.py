import camelcase

c = camelcase.CamelCase()

txt = "hello world"
print(c.hump(txt))
# Download a package named "camelcase": pip install camelcase
# Use the uninstall command to remove a package: pip uninstall
# Use the list command to list all the packages installed on your system: pip list
